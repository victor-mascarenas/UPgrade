export const todoReducer = (state, action) => {
    if (action) {
        switch (action.type) {
            // case 'add':
                
            //     break;
        
            default:
                break;
        }
    }
    return state;
};
