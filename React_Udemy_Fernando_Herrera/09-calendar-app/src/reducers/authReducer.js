const initialStte = {
    checking: true,/* 
    uid: null,
    name: null */
};

export const authReducer = (state = initialStte, action) => {
    switch(action.type) {
        default:
            break;
    }
    return state;
};